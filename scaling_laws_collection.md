# Scaling Law 论文合集（中文翻译）

本文档收录了大语言模型 Scaling Law（缩放定律）领域的核心论文翻译，按时间顺序排列。

---

## 目录

1. **Kaplan et al. (2020)** — 神经语言模型的缩放定律
2. **Hoffmann et al. (2022)** — 训练计算最优的大语言模型（Chinchilla）
3. **Zhen Guo (2024)** — 更多计算就是你需要的
4. **Lin et al. (2025)** — 如何使用缩放定律升级神经网络？一项综述

---

## 一、Kaplan et al. (2020)
### 《神经语言模型的缩放定律》
**Scaling Laws for Neural Language Models**
*Jared Kaplan, Sam McCandlish, Tom Henighan et al.*
*OpenAI / 约翰霍普金斯大学*
*arXiv: 2001.08361*

---

### 摘要

我们研究了语言模型性能（以交叉熵损失衡量）的经验缩放定律。损失与模型大小、数据集大小以及训练所用的计算量之间呈**幂律关系**，某些趋势跨越了超过七个数量级。网络宽度或深度等其他架构细节在很宽的范围内影响甚微。简单的方程控制了过拟合与模型/数据集大小之间的关系，以及训练速度与模型大小之间的关系。这些关系使我们能够确定固定计算预算的最优分配。更大的模型具有显著更高的样本效率，因此最优的计算效率训练涉及在相对适中的数据上训练非常大的模型，并在显著偏离收敛之前停止训练。

---

### 1. 引言

语言为人工智能研究提供了天然领域，因为绝大多数推理任务都可以用语言高效表达和评估，而世界上的海量文本为通过生成建模进行无监督学习提供了丰富的数据。深度学习近年来在语言建模方面进展迅速，最先进模型在许多特定任务上已接近人类水平表现。

我们预期语言建模性能取决于：模型架构、神经模型的规模、训练所用的算力，以及可用于训练的数据量。本工作我们实证研究了语言建模损失对所有这些因素的依赖关系，重点关注 Transformer 架构。

---

### 2. 核心幂律发现

#### 3.1 损失 vs 模型参数

**L(N) ∝ N^(-α)，α ≈ 0.076**

模型参数量每增加一个数量级，损失以可预测的幅度下降。

#### 3.2 损失 vs 数据集大小

**L(D) ∝ D^(-γ)，γ ≈ 0.095**

数据集大小对损失的影响比模型大小弱（γ < α），说明模型大小比数据量更重要。

#### 3.3 损失 vs 计算量（FLOPs）

**L(C) ∝ C^(-β)**

计算量与损失也存在幂律关系，跨越超过七个数量级。

---

### 3. 过拟合方程

过拟合程度由模型大小 N 与数据集大小 D 的比值决定：

**ε_overfit ∝ (N/D)^0.24**

当 N/D 较大时（模型相对数据过大），过拟合严重。当 N/D 较小时，过拟合可忽略。

---

### 4. 最优计算预算分配

结合 FLOPs 约束 C ≈ 6ND，最优分配为：

**N_opt ∝ C^0.73，D_opt ∝ C^0.27**

核心结论：
- 计算预算增加 10 倍 → 模型参数增加约 5.4 倍
- 计算预算增加 10 倍 → 训练数据增加约 1.9 倍
- **优先增大模型，而非数据**

---

### 5. Kaplan 的关键推论

1. **大模型样本效率更高**：在有限计算预算下，应优先训练大模型而非用更多数据
2. **提前停止至关重要**：不应训练至完全收敛
3. **架构选择影响很小**：深度 vs 宽度的选择在很宽范围内可以互换
4. **幂律跨越七个数量级**：这种关系具有极强的普适性

---

## 二、Hoffmann et al. (2022)
### 《训练计算最优的大语言模型》
**Training Compute-Optimal Large Language Models**
*Jordan Hoffmann, Sebastian Borgeaud, Arthur Mensch et al.*
*DeepMind*
*arXiv: 2203.15556*

---

### 摘要

我们研究了在给定计算预算下训练 Transformer 语言模型的最优模型大小和训练 token 数量。我们发现，当前的大语言模型严重欠训练（undertrained），这是因为近期人们倾向于在保持训练数据量不变的情况下扩展模型规模。通过在 50 亿到 500 亿 token 上训练了超过 400 个参数量从 7000 万到超过 160 亿的模型，我们发现：**对于计算最优训练，模型大小和训练 token 数量应该等比扩展——模型参数每翻一倍，训练 token 数量也应翻一倍。**

我们通过训练 Chinchilla 来验证这一假设。Chinchilla 与 Gopher 使用相同的计算预算，但参数量为 700 亿（是 Gopher 的 1/4），训练数据是 Gopher 的 4 倍。Chinchilla 在大量下游评估任务上均匀且显著地优于 Gopher（2800 亿参数）、GPT-3（1750 亿参数）、Jurassic-1（1780 亿参数）和 Megatron-Turing NLG（5300 亿参数）。在 MMLU 基准上，Chinchilla 达到 67.5% 的准确率，比 Gopher 高出超过 7%。

---

### 1. 背景与动机

Kaplan et al. (2020) 表明，自回归语言模型参数数量与其性能之间存在幂律关系。但 Kaplan 的著名结论是：大模型应该用相对较少的 token 来训练。

DeepMind 通过超过 400 个模型的实验发现这个结论是**错误的**。

---

### 2. 核心发现：Chinchilla 定律

Kaplan 建议：N_opt ∝ C^0.73，D_opt ∝ C^0.27

Chinchilla 新结论：**N_opt ∝ C^0.5，D_opt ∝ C^0.5**

即：模型参数和训练 token 数量应该等比增长。

### 2.1 数学模型

**L(N, D) = E + A/N^α + B/D^β**

其中：
- E ≈ 1.69（不可约 loss，即语言本身的熵）
- A ≈ 408.4，α ≈ 0.034
- B ≈ 408.4，β ≈ 0.034

**关键发现**：A ≈ B，α ≈ β！这意味着 N 和 D 对损失的贡献是对称的，因此最优情况下 N ≈ D。

---

### 3. 实验验证

DeepMind 训练了超过 400 个语言模型，涵盖参数量 70M ~ 16B，训练 token 5B ~ 500B。

### 3.1 Chinchilla vs 其他大模型

Chinchilla（70B 参数，1.4T token）对比：

| 模型 | 参数量 | 训练 Token | MMLU 准确率 |
|------|--------|-----------|------------|
| Chinchilla | 70B | 1.4T | **67.5%** |
| Gopher | 280B | 300B | 60.0% |
| GPT-3 | 175B | 300B | ~55% |
| Jurassic-1 | 178B | 300B | ~55% |
| MT-NLG | 530B | 270B | ~58% |

**结论**：Chinchilla 仅为 Gopher 1/4 的参数，却大幅超越 Gopher，证明"小模型 + 更多数据"优于"大模型 + 少量数据"。

---

### 4. Chinchilla 定律的实践意义

| 计算预算 | Kaplan 建议 | Chinchilla 建议 |
|---------|------------|----------------|
| 10B 参数模型 | ~205B token | ~200B token |
| 67B 参数模型 | ~1.5T token | ~1.4T token |
| 280B 参数模型 | ~7T token | ~7T token |

**核心原则**：每 1B 参数配备约 20B token 训练。

---

## 三、Zhen Guo (2024)
### 《更多计算就是你需要的》
**More Compute Is What You Need**
*Zhen Guo*
*MIT*
*arXiv: 2404.19484*

---

### 摘要

大语言模型预训练成本日益高昂，大多数从业者依赖缩放定律来分配计算预算到模型大小和训练 token，这通常被称为"计算最优"或"Chinchilla 最优"。在本文中，我们提出了一个新的缩放定律假说：**对于基于 Transformer 的模型，模型性能主要取决于所花费的计算量，而与模型大小和数据集大小的具体分配无关。**

基于这个统一的缩放定律，我们预测：
1. **对于推理效率**：训练应优先选择更小的模型和更大的训练数据集
2. **假设可用的网络数据集枯竭**：扩展模型大小可能是进一步提升模型性能的唯一途径

---

### 1. 背景与动机

Chinchilla 最优定律表明：模型参数每翻一倍，训练 token 数量也应翻一倍。然而，最近的研究趋势似乎打破了这一定律——以 Llama 3 为代表的一些模型用超过 Chinchilla 最优 10 倍以上的 token 进行训练，并表现出非常强的性能。

这引发了一个自然的问题：**Chinchilla 最优定律在自回归预训练中到底有多优？**

---

### 2. 新假说：统一缩放定律

之前的工作主要关注参数 N、数据集大小 D 和计算量 C 之间的关系：L = f(N, D)

**本文假设**：使用 BPC（bits per character，字符压缩效率）替代交叉熵损失来衡量模型性能。

**新公式：BPC = α·log(C) + β**

其中 C ~ N × D（参数十亿 × 训练token万亿）

**即：模型性能与计算量的对数呈线性关系，与具体分配无关！**

---

### 3. 实验验证

通过绘制多个开源强模型的压缩分数（BPC）与计算量的关系图，作者发现：

- 计算量（N × D）的对数与压缩分数呈**线性相关**
- 涵盖超过三个数量级的范围
- 线性拟合：α = -0.031，β = 0.572

**社区观察验证**：Llama-3-8B 与 Llama-2-70B 在预训练中花费了差不多相同的计算量，取得了相似的性能表现。

---

### 4. 关键预测

#### 预测 1：推理效率优先策略

对于推理效率，训练应优先选择：
- **更小的模型**
- **更大的训练数据集**

这样可以用相同的计算成本获得更好的推理效率（因为小模型推理成本更低）。

#### 预测 2：数据集枯竭后的策略

假设可用网络数据集枯竭：
- 扩展模型大小可能是进一步提升性能的唯一途径
- 因为无法通过增加数据量来 scaling

---

### 5. 局限性

1. **数据质量被忽略**：缩放定律未能考虑数据质量这一重要因素
2. **适用边界不清晰**：极端的比例似乎不切实际
3. **评估指标局限**：BPC 可能无法完全捕捉模型能力的所有方面

---

## 四、Lin et al. (2025)
### 《如何使用缩放定律升级神经网络？一项综述与实践指南》
**How to Upscale Neural Networks with Scaling Law? A Survey and Practical Guidelines**
*Lin et al.*
*arXiv: 2502.12051*

---

### 摘要

神经缩放定律彻底改变了大规模 AI 模型的设计和优化，它揭示了模型大小、数据集规模和计算资源之间可预测的关系。然而，**最近的研究揭示了这些定律在跨架构、跨模态和跨部署场景下的局限性。**

稀疏模型、混合专家（MoE）、检索增强学习和多模态模型往往偏离传统缩放模式。此外，缩放行为在视觉、强化学习和微调等领域也各有不同。本综述综合了超过 50 项研究的见解。

---

### 1. 经典缩放定律的局限

#### 1.1 传统幂律的失效

经典缩放定律在以下场景中失效或需要修正：

- **稀疏模型**：稀疏激活或稀疏权重的模型行为与 dense 模型不同
- **混合专家模型（MoE）**：FFN 层被多个专家替代，scaling 行为不同
- **检索增强模型**：性能不仅取决于模型参数，还取决于检索系统的质量
- **多模态模型**：需要同时考虑文本、图像、音频等多种模态的 scaling

---

### 2. 实践指南

### 2.1 模型设计决策

1. **从小型模型开始**：先在小规模验证假设
2. **找到最优 token/参数比**：通过实验找到适合你数据质量的最优比率
3. **考虑推理成本**：Chinchilla 最优从训练角度最优，但推理角度小模型更优

### 2.2 计算分配

| 场景 | 推荐策略 |
|------|---------|
| 训练成本敏感 | Chinchilla 最优（等比 scaling） |
| 推理成本敏感 | 小模型 + 更多数据 |
| 数据质量高 | 可以偏离 Chinchilla 最优 |
| 多模态 | 需要分别考虑各模态的 scaling |

---

## 五、其他重要 Scaling Law 相关论文

### 5.1 《超越神经缩放定律：通过数据剪枝击败幂律缩放》
*Beyond neural scaling laws: beating power law scaling via data pruning*

核心观点：通过智能数据剪枝（选择最有价值的训练样本），可以在相同计算预算下获得更好的性能。

### 5.2 《破碎的神经缩放定律》
*Broken Neural Scaling Laws*

核心观点：许多声称的 scaling law 在更广泛的数据集和架构中并不成立，实际的 scaling 行为往往是非线性的或存在"断点"。

---

## 六、Scaling Law 演进总结

| 年份 | 论文 | 核心结论 |
|------|------|---------|
| 2020 | Kaplan et al. — 神经语言模型的缩放定律 | 大模型比大数据更重要；N_opt ∝ C^0.73 |
| 2022 | Hoffmann et al. — Chinchilla | 模型和数据应等比扩展；N_opt ∝ C^0.5 |
| 2024 | Guo — 更多计算就是你需要的 | 性能主要由计算量决定，与分配无关；BPC = α·log(C) + β |
| 2025 | Lin et al. — 综述 | 传统幂律在 MoE、多模态等新场景下失效 |

---

## 七、实践建议总结

### 如果你的目标是**最低训练成本**：
→ 遵循 Chinchilla 最优：每 1B 参数配 20B token

### 如果你的目标是**最低推理成本**：
→ 训练更小的模型 + 更多的数据（Guo 2024 的发现）

### 如果你的**数据质量很高**：
→ 可以用比 Chinchilla 最优更少的 token 训练

### 如果你使用 **MoE 或稀疏模型**：
→ 传统 scaling law 不适用，需要专门的分析

### 如果你做 **多模态训练**：
→ 需要分别考虑各模态的 scaling 特性

---

*本合集由 AI 辅助整理翻译，原始论文请参阅各自 arXiv 链接。*
